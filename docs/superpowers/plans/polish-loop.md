# Polish loop — GOAP plan and checklist

**Goal:** every screen reviewed by three specialised sub-agents (visual design,
motion & interaction, UX/copy/correctness), blocker + should findings applied,
re-captured light + dark at 390×844, committed on `redesign`; lint/build and
API tests green; screenshots refreshed; pushed and deployed.

**Current state (2026-09-17):** `redesign` at `a7b2091`, live at
https://app-production-f91d.up.railway.app. Local: API :8010, web :5176, demo DB
re-seeded with one upcoming event (Semana Santa, Antigua, in 9 days).

**Plan cost:** 12 screen tasks + 1 cross-cutting task; 3 reviewers each;
sequential fixes so the working tree stays coherent.

**Risk factors:** a fix breaks lint/build or another screen → revert, log,
replan; captures fail → restart the dev server by PID, retry once; deploy fails
→ report, don't loop.

**Fallback:** reviews sequential with the Agent tool if parallel dispatch fails.

## Checklist

| # | Screen | Status | Findings (A/B/C → applied) | Commit |
| --- | --- | --- | --- | --- |
| 0 | Cross-cutting (tokens, shared components) | done | 401 rewrite, 422 text, btn hover/press/busy, code font | a0f58b7 |
| 1 | Sign in | done | A 8 / B 8 / C 8 → 20 applied, 2 skipped (drag momentum, shared-layout wordmark) | 4036cbb |
| 2 | Home | done | A 8 / B 8 / C 8 → 22 applied, 2 skipped (skeleton shape partly; nothing else) | b05e01e |
| 3 | Map | done | A 8 / B 8 / C 8 → 22 applied, 2 skipped (sheet height→transform refactor; row/pin parity beyond select) | d40b0d7 |
| 4 | Saved (tabs) | done | A 8 / B 4 / C 8 → 18 applied, 2 skipped (API failure copy; drop Places tab) | a66c7d7 |
| 5 | Inbox review card | done | A 8 / B 4 / C 8 → 18 applied, 2 skipped (Ignore undo endpoint; stagger cap in MotionRow) | a66c7d7 |
| 6 | Place | done | A 8 / B 8 / C 8 → 23 applied, 1 skipped (a visit sheet with date + rating) | c9e9079 |
| 7 | Trip | done | A 8 / B 8 / C 8 → 24 applied, 1 skipped (rotation hand-off between the two globes) | e5ab969 |
| 8 | Journey | done | A 8 / B 8 / C 8 → 24 applied, 2 skipped (vaul snap-point drawer; reorder stops) | 4ea1481 |
| 9 | Ask drawer | done | A 8 / B 8 / C 8 → 24 applied, 0 skipped | 5339ad8 |
| 10 | Save drawer | done | A 8 / B 8 / C 8 → 23 applied, 1 skipped (a tinted map strip on Here) | d041095 |
| 11 | You / profile | done | A 8 / B 8 / C 8 → 22 applied, 2 skipped (HeroActions on the map bar; reduced-motion pill fade, on-spec) | 3c559ab |
| 12 | New trip + empty states | done | A 8 / B 8 / C 8 → 23 applied, 2 skipped (PATCH /trips for dates and budget; Ask starters aware of an empty stash) | (this commit) |

## Trajectory

- Step 0: plan written; servers up; demo DB re-seeded.
- Step 1 (cross-cutting): reviewers proved a 401 rewrite bug on sign-in (any 401 → "session expired"); fixed in api.ts with a test-by-hand (wrong password now reads "Wrong email or password."). Commit a0f58b7.
- Step 3 (home): API — a stand-in origin (destination centre) no longer produces "from you" distances; resurfaced cards deduped; test added. Web — cold-load entrance vs tab-switch slide was inverted by `initial={false}` on AnimatePresence (Motion memoises presence context): fixed with a module-level cold flag; back-navigation now slides the right way (custom variants); `on` sent as the local date; deep links to Trip sections; queue card quiet (plain card, title-size numeral, muted/warn stamp); hero washes removed; money card on teal-deep with AA text; gold glyph readable in dark; `--ink-3`/`--tint-view` darkened for AA; rail cards equal height, foot only when it has content; section rhythm fixed; press feedback on card links; skeleton held 150 ms and cached payload on return; FAB lifts by transform; rain in the weather chip; chip is a full tap target; footer is a sentence that links to Saved. Side note: a reviewer's itinerary test flipped one demo place to "planned" (planner only promotes) — demo DB will be re-seeded before final captures; the non-reverting promotion goes to the Trip review.
- Step 4 (map): blocker from all three — the sheet's inline height was never released after a pin tap, clipping the recall card and hiding Navigate under the tab bar. Rewrote MapScreen: one MotionValue drives both list and content-sized card (measured by ResizeObserver, capped at 60vh); markers kept by id (no re-mount on tap/filter/keystroke; drop-in only on first draw); 44px hit areas; status badge icons and a solid coral must-visit disc; favourite ring readable on light tiles; overlapping pins cluster into a count pin that zooms; locate button flies to you and shows a note when denied; honest titles/empty states for search vs filters vs loading; markers and the close button accessible; row tap opens the card like the pin. API: navigation handoff no longer forces walking mode for places beyond walking range. CSS: FAB/locate/attribution follow the sheet via `translate` (motion's whileTap owns `transform`), no transition while the sheet is live; tint pane pulls tiles into the palette (multiply light / lighten dark); ring pulse plays twice; reduced motion also zeroes animation delays.
- Step 2 (sign in): scene no longer rebuilds at landing (spin via ref); no drop-shadow/backdrop-blur over the live canvas; pixel ratio capped 1.5; intro 1.1 s, skipped for returning users, tap to skip; CTA dark-on-teal (AA); space palette now overrides danger/warn; error slot reserved with role=alert and aria-invalid; busy button breathes; dev-only demo hint; email remembered; logo compressed to 0.85 s and starts after the form lands; drag works under reduced motion. Verified: one canvas, intro skipped on return, error text correct in both schemes.
- Step 5 (saved + inbox, one commit): API — three bugs proven live by reviewer C, each now under test: a source never settled to "completed" (the decided candidate was not flushed before the pending check); an edit on a tip changed only the shortened title, and an edited place name was discarded (card now edits what is shown, sends `override_name`, and the candidate remembers `user_edited`); a note typed on a tip was silently dropped (now appended under the claim, item marked yours); duplicates now name the place they match. Web — review card: the creator's sentence *is* the headline (gold lead quote with "— from the article / your note / caption at 1:24"), edit and note are toggles beside the stamp, one decision row (Stamp / Ignore), stamp lands "Pinned" for a place and "Saved" for a tip, in-place editor with the headline's metrics, note opens by grid track; exit keys unified so the card actually fades before the list reloads, tick lands with the stamp (170 ms), hold 700 ms, exits ease-in; groups by source (headers are the video/article, the stamp says the type), section and card exits animate with popLayout, ignore no longer widens the page. Saved shell: panes slide along the segmented axis with tweens (no 240 ms blank), each view staggers once then is cached (`useAsync` cacheKey), Inbox tab carries the queue count, real tabs (roving tabindex, arrows, tabpanel), `?tab=` and `?source=` deep links. Places: "saved" stamp gone (only exceptions get one), meta = city · walk · sources>1, search debounced with clear button and an honest "Nothing named …" state. Knowledge: stamp-then-headline rows, provenance and dates in words, "Stay" filter added, events in date order with past ones dimmed, archive with 5 s Undo and an "Archived" chip to get things back. Sources: unexpected states only (Failed / Processing / "N to review" → Inbox filtered by that source), never-blank names, honest delete confirm built from the row, Retry for failed and empty captures, quiet text-only Delete. Tokens: AA gradient/teal/coral inks in light, dark toggle gets `--gold`, stamp-sm 10px, confidence track visible, reduced motion keeps a 150 ms fade and haptics. Skipped: API failure-reason copy (not proven live; next pass), Ignore-undo needs a restore endpoint, dropping the Places tab, an 8-row stagger cap inside MotionRow.
- Step 6 (place): cross-cutting motion bugs proven by reviewer B — the entering page never slid (`custom` on AnimatePresence reaches only the exit; the page now carries its own) and the browser restored scroll under the page still leaving (restoration is manual now; App scrolls after the exit, to 0 on push and to the remembered offset on pop). Place: a fixed bar whose glass and title fade in while the big title fades out, with a Back that is always live (deep links go to Saved → Places instead of out of the app; a 404 gets its own "Gone" state); the status picker is the screen's one moment — local state answers the tap, the corner stamp lands with the tick, only the pressed stamp is busy, five stamps in two rows; failed writes no longer lock the page (try/finally, optimistic revert, honest note); "Add to today" is a named toggle ("In today's plan") that uses the local date, tells you where it went and reverts the promotion when the last row leaves; "Visited" records a visit so the record agrees with the stamp; the reason is said once with its author instead of twice in gold; live facts have labels, pressable website/phone, "$$ · mid-range", provenance in words, the adapter name gone, fresh in the grey line and only ageing stamped; weather is the gold line; naive UTC timestamps parsed as UTC ("checked 30 min ago", not "3 h"); category, address and dates in words, coordinates moved to a copy row; the hero map is tinted like the big map (shared helper); favourite is glass, optimistic, no transform fight; parallax and bar slide off under reduced motion; skeleton shaped like the page and held 150 ms; filled stamp text white in light. Skipped: a "When did you go?" sheet with rating (Trip/visits pass).
- Step 7 (trip): API, proven by reviewer C and under test — removing the last plan row now reverts the promotion the plan made (the map stamp lied); `25:99` is no longer a valid time; untimed rows go last, as Home already had them; bookings and expenses can be deleted (a mistyped 7,200 no longer sits in the total for the rest of the trip). Web — the export is a real download (a plain link could not carry the token: it downloaded a 401), used on the profile too; every form catches failure, keeps what was typed and says what happened; rows are removed optimistically with a tick and an exit; the journey card says the one thing nothing else does ("Day 22 of 117 · Lake Atitlán next") and its stamp keeps the night palette in light; the stepper drops its second coral stamp; "Account" is gone from the trip (it is the profile's); dates and enums in words (hero, day headings as h3 with Today/Tomorrow, expense chips/meters/rows, booking kinds with a picker); money adds up — categories in the trip's currency as a share of spend, foreign rows show "≈ 13 USD", over-budget shows the real percentage, "Over by …" in coral and a coral arc; the arc's stroke and its number run on one spring; NumberFlow lands with the arc (450 ms); meters grow once per visit by scaleX; section switch exits on the 150 ms tween (no blank gap), enters even for the first section (`initial={false}` on AnimatePresence had frozen every descendant's entrance); reduced motion keeps fades and gets ticks; real tabs with panels. Globes — the photographed Earth fades in over the vector one once its texture is on the sphere (shared `Planet`, one cached texture), pins/route rebuild in place instead of tearing down the renderer, and both globes stop drawing off-screen (0 draws measured). Skipped: rotation hand-off between vector and WebGL globes (covered by the cross-fade).
- Step 8 (journey): API, proven by reviewer C and under test — `/recommend` matched accents on one side only, so "Lake Atitlán" and "Lanquín" answered "nothing stashed" (both sides normalised now), and cards come warnings → next event → places → notes, as the design says. Globes — the vector stand-in and the Earth now read one shared clock, turn the same way (east, like the real one) and centre the focus with the same tilt, and the stand-in stays under the Earth through its fade, so the hand-off is a cross-fade instead of a blink and a jump; the flight is keyed on the leg alone (a trip reload no longer restarts it); legs too short to fly land their bead instead; the plane is gold and bigger; a let-go keeps the hand's speed then eases into a sway that keeps the route in view (no more turning it away); marks scale with the camera; Journey zooms to the route's span with a coral bead for where you are; the per-frame drop-shadow on the WebGL canvas is a static glow. Journey screen — one night token block (`.night`) shared with sign-in and the trip card so no paper token leaks in light; title on a scrim instead of a pill; glass chips that bleed the edge, quiet numerals, current stop by coral hairline + stamp, scrolled into view; stash panel replaces the rail (planet stays), keyed by id, with a 44 px teal "I'm here now", a landing stamp with the tick, Escape/close with focus return, "Remove stop", place rows as links, category labels, "See all N in Saved"; search never claims "no match" before looking (pending flag), says when it could not look, filters Nominatim to places (no amenities or countries), dedupes, shows the region, results grow upward so the field never moves, Enter waits for the lookup; a twin stop ("Antigua Guatemala", or within 15 km) is refused with a pointer to its chip; Back goes to the trip; combobox/listbox roles and a live region; the viewport resizes with the keyboard. Skipped: a vaul drawer with snap points (the fixed-height glass panel covers it), reordering stops (the API has no position edit).
- Step 9 (ask): API, proven by reviewer C and under test — "what did I save about safety" was classed as a "why" question (subject now decides before phrasing); a named place in the question is found without context or a location (no more "I do not have your location" for "Is now a good time for Cerro de la Cruz?"); a place 318 km away is "not a walk", not "4244 min walking"; "Tell me about Tokyo" no longer matches a note on the word "about" (question words dropped, two content words or a place name required); confirming the same proposal twice plans it once; proposals keep their name ("Add … to today's plan" → "Add to today" → "Added to today's plan"), dates and plurals in words, budget prose rounded like the card. Drawer — focus goes into the field on open (autoFocus after the portal) and back to what opened it on close; the assistant's sheet has one fixed height so the frame never snaps with the answer (body scrolls); the rise-in choreography plays once at open, not on every answer; reduced motion keeps a 150 ms fade instead of a pop; the grip is visible. Ask — pills ask (one tap), per-surface starters that wrap, a seeded question is asked at once; one context chip with an × to drop it and a "Use my location" chip when there is none; the answer is a real sentence (sr-only text + spaces) in a live region, re-revealed per answer and cleared while the next one is read (no stacking); the send disc is 44 px, plays on send and stops when the answer lands; the field grows with the question and Enter sends; errors sit under the field with Retry; the proposal is a decision (dashed ring, "Add to today" 44 px + "Not now", pending/error, the one stamp moment "Added" with the tick, then "View today's plan" and an honest footer "1 change made"); knowledge cards show the whole sentence under a type label; place cards use category/provenance words, hide the reason when it is the citation, keep "checked" once in the meta and stamp only stale facts; budget figures use one formatter; the handoff note sits with the links; cards stagger in after the text.
- Step 10 (save): API, proven by reviewer C and under test — "Here" now keeps the coordinates (the sheet showed them and threw them away): a manual capture with a position becomes a place candidate in Inbox whose options always include "exactly where you stood" (provider `you`), so it can be approved with a real pin; a repeat capture answers 200 with `duplicate: true` instead of looking like a failure; a batch upload is checked file by file before anything is stored (no more half-committed batches); `.vtt` accepted; failure copy names Sources, where the original actually is. Drawer — reviewer B proved the X never reached `onClose` (vaul only reports its own closes), so one X-close left the sheet mounted and the FAB dead: the X and every in-sheet "Done" now close through vaul (`DrawerClose`), so the slide-out, the focus return and `onClose` all run; a `subhead` slot keeps the kind switcher above the scrolling body; reduced motion fades via an animation the presence layer waits for; vaul's slide is 400 ms. Save — one fixed height (`drawer--save`) with the switcher and the primary at the same y for every kind (also with the keyboard up), panes slide in the pill's direction; each kind keeps its own words (a note no longer leaks into a link), errors clear on switch, a caption without a link is saved as a note, the link kind requires a link; Media: the picker offers exactly what the API accepts, refuses oversize/unsupported files before sending with plain copy, shows thumbnails with × and names/sizes, has drag-over/chosen states and a focus ring; Here: "Use my location" pill with a locating state, the coordinates line says where it goes, "Name this place", "Stash this place"; Event: past-date warning, Until validation in words, 240-char cap, stamp "On the trip", "Save another"; outcome: the capture shown as what it is (glyph, title, "Note · 1 to review") with the xl stamp landing on its corner, the consequence (note + buttons) following the landing, one haptic, "Review in Inbox" → the inbox filtered to that source, "Open in Sources" for a failed or duplicate capture with honest copy, the URL kept for a retry; busy breathes instead of dimming, a disabled primary is quiet paper, 44 px segmented targets, role=status / role=alert, offline copy. Skipped: a tinted static map strip on Here (the Place header pattern) — the coordinates line and the Inbox pin cover it.
- Step 11 (profile + RTL): reviewer C proved another account's cached Home and Inbox showed after sign-out until the new fetch landed — sign-out now forgets every remembered payload and Home's cache and closes every sheet; the account is fetched once (context) instead of per header; the sheet slides out before the scene changes and sign-in fades in; Hebrew no longer mangles English (every sentence keeps the direction of its first letter — `unicode-bidi: plaintext` — numbers stay LTR) while the chrome mirrors properly: logical inset/padding/margins for the FAB, place stamp, coordinates, drawer head, thumbnails, send disc; chevrons and arrows flip; page slides follow the reading direction; the segmented pill snaps across a direction flip instead of gliding away from the finger and its arrow keys follow the reading direction; dates and money now honour the chosen language (the note that claimed so is true, and it is written in each language); a theme switch is one cut (no control eases its own colour after the paper flips), the scheme and direction apply before first paint (inline script) and the browser chrome follows the chosen scheme; the avatar is an inked disc (16:1) instead of white on a coral gradient (2.3–3.7:1); theme and language are radio groups; group labels sit under the account instead of outranking it; the slogan stamp is plain text; export holds its busy state, says "Exported" and "Downloaded tripstash-export.json.", and a dead token reads as a session expiry; the export's place and source rows carry ids so evidence joins (API, under test); vaul's 500 ms slide is now really 400. Skipped: HeroActions on the map's glass bar; a reduced-motion fade on the pill (on-spec as an instant change).
- Step 12 (new trip + empties): API, proven by reviewer C and under test — the daily budget divided the money by the days until the end, counting the days before the trip (a 30-days-out, 8-day trip read "26 a day for 38 days"; now 125 for 8); "123" is not a currency and 1e300 not a budget; Ask with no stop no longer searches the trip's name as a place, and its "no location" copy names the app's own actions. Web — reviewer B proved every sign-in flashed New trip for a frame before the skeleton (`useAsync`'s loading flag only turned true in an effect): loading is now known in the same render the inputs change, and New trip is shown only on the answer "no trip" and stays up through its own reload; the app skeleton is shaped like Home and held; New trip: the stamp moves from the screen label to the moment (the trip name lands on the card, the tick, then the page gives way and Home gets its cold-load entrance), a budget typed like "1,000" or "$500" is a number (it was silently dropped), a currency must be three letters, dates validate in words, focus starts in the name, busy breathes instead of dimming, a disabled ink primary is quiet paper, Sign out is a quiet link top-right, copy no longer promises dates can be added later; empties: no stamp unless earned (only "All clear" after a cleared queue keeps one), each is an h2 + one line + one ink "Save something", enters like a row, clears the FAB; the fresh Inbox says "Nothing to review yet" (not "everything reviewed"), Places hides its search and Knowledge its eleven chips over nothing, Map opens on you / the current stop / the world instead of Antigua and its sheet sizes to the empty state with the trip's name, a locate timeout is not blamed on permission, Trip's card says "No dates · Add your first stop" and shows no Day plan before there is a route, Money says "Nothing spent yet", Journey with no stops shows the whole planet under "Where does it start?"; Home's eyebrow no longer repeats the headline and its line says "No dates yet" / "Tomorrow"; the skeleton hold survives reduced motion; the FAB and tab bar leave with the page; Trip's hero rises once, on a cold load; cold-load resets on sign-out. Skipped: PATCH /trips (dates/budget after creation) and starters that know the stash is empty.
